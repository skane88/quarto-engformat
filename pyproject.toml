[project]
name = "quarto-engformat"
version = "1.0.0"
description = "Repository for engineering calculation formats for Typst"
readme = "README.md"
dependencies = [
    "notebook",
    "numpy",
    "pandas",
    "matplotlib",
    "polars>=1.43.1",
    "ruff>=0.16.1",
]

[tool.ruff]
# Add a custom file extension mapped to Python
extension = {qmd="markdown"}